
2021 season:
Log request/response
  correlation Id
  log calls into controller and module to module calls
  redact fields that are marked
Player PII
  Admin can see all player info and does a put to update
  Non-admin can only see his info and others are masked and does a put to update his own
After changing email get an error although the email has changed
Remove spring fox?
CI/CD again
HATEOS?

2022 season:
Add seasonId to the all the game urls
Composite primary key for game
UUID for keys (no longer auto increment)

====== DONE =======
NGINX
  frontend file server
  reverse proxy backend
  SSL termination
  cache headers index.html
Dockerize
  Backend/database
Test deployment
  VM
Dockerize
  Dockerfile and docker-compose.yml so that server can run locally with H2
    mvn -P h2-embedded-tomcat-spring-integration -pl application clean package
    docker build -t texastoc-v4-server-image .
  Remove REST calls (that can replace events)
    mvn -P h2-embedded-tomcat -pl application clean package
Clock controls more intuitive
UI settings for one touch rebuy
Move past season history out of json file
Get rid of the season estimated payouts
Refactor cucumber tests into JUnit tests.
Travis CICD
  Heroku deploy - rest calls instead of messaging?

moved to zsh
  https://scriptingosx.com/2019/06/moving-to-zsh/

Java 11
  brew tap AdoptOpenJDK/openjdk
  brew install --cask adoptopenjdk8
  brew install --cask adoptopenjdk11
  brew install jenv
  Added to .zshrc
    export PATH="$HOME/.jenv/bin:$PATH"
    eval "$(jenv init -)"
  close all terminals and start a new terminal
  jenv enable-plugin export
  jenv enable-plugin maven
  close all terminals and start a new terminal
  for version in 8 11
  do
     jenv add /Library/Java/JavaVirtualMachines/adoptopenjdk-$version.jdk/Contents/Home
  done
  jenv versions
  jenv global 11
  jenv global
